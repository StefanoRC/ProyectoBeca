---
import MainLayout from "../layouts/mainLayout.astro";
import { games } from "../data/games.js";
---

<MainLayout title="GamesQuiz">
  <header>
    <h1>GamesQuiz</h1>
    <button id="menu-toggle" class="menu-button">☰</button>
    <nav id="mobile-menu" class="hidden">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/userRegister">Register/Login</a></li>
        <li><a href="#catalog">Catálogo</a></li>
        <li><a href="/contact">Contacto</a></li>
      </ul>
    </nav>
  </header>

  <main>
    <section>
    <h2>🎮 Bienvenido a GamesQuiz</h2>
    <p>GamesQuiz es un desafio donde pondrás a prueba tus conocimientos sobre videojuegos y lograras desbloquear mas desafios a medida que avances. Ve por un buen cafe y comienza a demostrar tu valia gamer!</p>
    </section>

    <section>
    <h3>📜 Reglas del juego</h3>
    <ul>
      <li>Responde correctamente las preguntas para ganar puntos.</li>
      <li>Necesitás alcanzar el puntaje mínimo para desbloquear cada juego.</li>
      <li>Si pierdes todas tus vidas, no te preocupes, puedes intentarlo otra vez. Advertencia: No esta permitido buscar en Google. Recuerda que Dios lo ve todo XD.</li>
    </ul>
    </section>

    <section>
      <h3>📦 Catálogo de Juegos</h3>
      <div id="catalog" class="card_container">
        {games.map(game => (
          <div class="card"  data-game-id={game.id} data-game-idalt={game.idalt}>
            <div>
              <img src={game.image} alt={game.name} />
              <a class="btn_anchor" href={`/${game.id}`}>
                <button class="btn card_item">Jugar</button>
              </a>
            </div>
            <p class="card_item">Valor: {game.scoreToUnlock} créditos</p>
          </div>
        ))}
      </div>
    </section>
  </main>
  
  <footer>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="/contact">Contact</a></li>
      </ul>
    </nav>
    <p>&copy; 2025 GamesQuiz</p>
  </footer>
    <script type="module">
      document.addEventListener("DOMContentLoaded", () => {
        const unlockedGames = JSON.parse(localStorage.getItem("unlockedGames") || "[]");

        document.querySelectorAll(".card").forEach(card => {
          const gameId = card.dataset.gameId;
          const gameIdAlt = card.dataset.gameIdalt;
          if (unlockedGames.includes(gameId)) {
            const anchor = card.querySelector("a");
            const button = card.querySelector("button");

            anchor.href = `/${gameIdAlt}`;
            button.textContent = "Detalles del juego";
          }
        });
      });
</script>
</MainLayout>

<style>
  /* SEMANTICS */
    body {
    margin: 0;
    background-color: var(--bg-color);
    color: var(--text);
    display: grid;
    grid-template-rows: auto 1fr auto; /* ✅ Esto lo organiza en filas */
    grid-template-areas:
      "header"
      "main"
      "footer"; /* ✅ Ajuste aquí */
    height: 100vh;
    font-size: x-large;
  }


  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem;
    background-color: oklch(0.21 0.1209 279.86);
    box-shadow: var(--shadow);
    grid-area: header;
  }

  footer {
    grid-area: footer;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 1rem;
    background-color: oklch(0.21 0.1209 279.86);
    box-shadow: var(--shadow);
    flex-direction: column;
  }

  section {
    border: 1px solid var(--primary);
    padding: 1rem;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    margin: 1rem;
    background-color: oklch(0.21 0.1209 279.86);
  }

  /* CARDS */

  img {
    max-width: 100%;
    height: auto;
  }

  .card_container {
    display: flex;
    flex-wrap: wrap;
    flex-direction: row;
    gap: 1rem;
    justify-content: center;
  }

  .card {
    background: #111;
    border: 1px solid var(--primary);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    display: grid;
    grid-template-rows: auto auto;
    width: 250px;
    height: auto;
    overflow: hidden;
    justify-items: center;
    align-items: center;
    transform: scale(0.95);
  }

  .card:hover {
    transform: scale(1);
    transition: all 0.2s ease-in-out;
  }

  .card img {
    border-radius: var(--radius) var(--radius) 0 0;
    width: 100%;
    height: auto;
    margin: 0;
  }

  .card_item {
    margin: 5px;
    padding: 5px;
  }

  .card div {
    position: relative;
  }

  .card .btn_anchor {
    position: absolute;
    border: none;
    opacity: 0;
    transition: opacity 0.2s ease-in-out;
    cursor: pointer;
    left: 50%;
    bottom: -5%;
    transform: translateX(-50%);
    z-index: auto;
    width: auto;
  }

  .card:hover .btn_anchor {
    transition: all 0.2s ease-in-out;
    opacity: 1;
    bottom: 5%;
  }

  /* MENU */
  .menu-button {
  font-size: 2rem;
  background: none;
  border: none;
  cursor: pointer;
}

#mobile-menu {
  position: absolute;
  top: 120px;
  left: 0;
  width: 100%;
  background: var(--bg-color);
  padding: 1rem;
  display: none;
  flex-direction: column;
  z-index: 10;
}

#mobile-menu ul {
  list-style: none;
  padding: 0;
}

#mobile-menu li {
  padding: 10px;
}
</style>

<script>
document.addEventListener("DOMContentLoaded", () => {
  const menu = document.getElementById("mobile-menu");
  const toggleBtn = document.getElementById("menu-toggle");

  toggleBtn.addEventListener("click", () => {
    if (menu.classList.contains("hidden")) {
      menu.classList.remove("hidden");
      menu.style.display = "block";
    } else {
      menu.classList.add("hidden");
      menu.style.display = "none";
    }
  });
});
</script>